#include <stdio.h>

int main() {
    char filename[] = "7-print_tebahpla.c";

    if (remove(filename) == 0) {
        printf("File '%s' deleted successfully.\n", filename);
    } else {
        perror("Error deleting the file");
    }

    return 0;
}

